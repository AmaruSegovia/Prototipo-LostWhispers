using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UcumarAppear : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        if (5 > 2)
        {
            Debug.Log("El numero es mayor");
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
